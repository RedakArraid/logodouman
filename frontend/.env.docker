# Variables d'environnement pour le frontend dans Docker
# Ce fichier est utilisé par le container frontend

# URL de l'API backend (TOUJOURS localhost pour le navigateur)
NEXT_PUBLIC_API_URL=http://localhost:4002

# URL du site (pour les redirections)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Environnement
NODE_ENV=production

# Configuration pour le build
NEXTAUTH_SECRET=logodouman-nextauth-secret-docker
NEXTAUTH_URL=http://localhost:3000
